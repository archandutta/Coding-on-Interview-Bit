int titleToNumber(char* A) {
    int i,len,e=0,res=0,asc;
    len=strlen(A);
    //printf("%d ",len);
    for(i=(len-1);i>=0;i--,e++)
    {
            asc=A[i]-64;
            res=res+asc*(int)pow(26,e);
        
    }
    return res;
}
